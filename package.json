{
  "name": "maestro-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @maestro/api dev & pnpm --filter @maestro/ui dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @maestro/db generate",
    "db:migrate": "pnpm --filter @maestro/db migrate",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "format": "prettier --write \"packages/*/src/**/*.ts\"",
    "prepare": "node scripts/prepare-env.js"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.58.0",
    "@typescript-eslint/parser": "^8.58.0",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0"
    }
  },
  "author": "Naoto Kudo <naoto@kudos.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/naotantan/maestro"
  },
  "bugs": {
    "url": "https://github.com/naotantan/maestro/issues"
  },
  "keywords": [
    "ai",
    "agent",
    "multi-tenant",
    "budget-monitor",
    "claude",
    "gemini"
  ]
}
